package guise.states;


class ControlStates {}
enum ControlActiveState		{ ACTIVE; INACTIVE; }
enum ButtonOverState		{ OVER; OUT; }
enum ButtonDownState		{ DOWN; UP; }
enum SelectedState			{ UNSELECTED; SELECTED; }
enum SelectableState		{ UNSELECTABLE; SELECTABLE; }
enum FocusState				{ FOCUSED; UNFOCUSED; }
enum HScrollState			{ ACTIVE; INACTIVE; }
enum VScrollState			{ ACTIVE; INACTIVE; }